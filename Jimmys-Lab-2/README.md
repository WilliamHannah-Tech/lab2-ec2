Script logic

This startup script uses Bash to automatically set up an EC2 server when it starts. The script installs the web server and makes sure it runs automatically. It then creates a web page with text, styling, and an image in the public web folder. Because the script runs at startup, the website is available right away without needing to log in to the server.